{
  "name": "chrome-extension-ts-starter",
  "version": "1.0.0",
  "license": "MIT",
  "description": "Chrome Extension to create a new visualization for the Steam Booster Pack Creator page",
  "scripts": {
    "build": "parcel build src/content/*.ts --dist-dir dist/content --no-source-maps",
    "watch": "parcel watch src/content/*.ts --dist-dir dist/content --no-source-maps",
    "copy:static": "copyfiles manifest.json assets/**/* dist",
    "clean": "rimraf dist",
    "dist": "npm run clean && npm run build && npm run copy:static",
    "lint": "eslint --ext .ts,.tsx ./src",
    "test": "jest -c ./config/jest.config.js"
  },
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.7.0",
    "@parcel/transformer-typescript-tsc": "^2.7.0",
    "@types/chrome": "^0.0.197",
    "@types/jquery": "^3.5.14",
    "@types/node": "^18.8.2",
    "@typescript-eslint/eslint-plugin": "^5.39.0",
    "@typescript-eslint/parser": "^5.39.0",
    "copyfiles": "^2.4.1",
    "eslint": "^8.24.0",
    "jquery": "^3.6.1",
    "parcel": "^2.7.0",
    "path-browserify": "^1.0.1",
    "process": "^0.11.10",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "posthtml-parser": "^0.11.0",
    "posthtml-render": "^3.0.0"
  }
}
